#include <stdio.h>
#include <algorithm>
using namespace std;
int n, k;
int arr[100000];
int a, b;
int main() {
	scanf("%d%d", &n, &k);
	for (int i = 0; i < n; i++) {
		scanf("%d%d", &a, &b);
	}
	printf("비와이\n");
	return 0;
}